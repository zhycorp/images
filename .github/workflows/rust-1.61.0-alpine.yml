name: Build & Push rust/1.61.0/alpine to Image Registry

on:
  push:
    branches:
      - main
    paths:
      - "rust/1.61.0/alpine/**"
  pull_request:
    branches:
      - main
    paths:
      - "rust/1.61.0/alpine/**"
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      root_directory: rust/1.61.0/alpine
      work_directory: rust